# My dotfiles

* tmux configuration
* local bash changes

    ln -s bashrc ~/.bashrc
    ln -s tmux.conf ~/.tmux.conf
    ln -s cvsignore ~/.cvsignore
    git config --global core.excludesfile '~/.cvsignore' 
